# Mongus-ClothGame
 This is my very first Java project I've done after watching a 12 hours Java course and getting some looks at stack overflow.
 The code is very nasty being all around the place and quite repetitive, so get a bucket before you throw up!
 I'm actually very proud of it! It took some headache to do, specially with the string.equals and do while stuff (Why do I have to do str.isEqual() instead of just using == :broken_heart:), but it came pretty alright in the end.